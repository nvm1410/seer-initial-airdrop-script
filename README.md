Install packages:
```
yarn
```

Run
```
node index
```

The exported data will be in the data folder. This is the CSV structure:

| Address | Is POH User | Timestamp | Total Holding (sDAI)

 The script will get 30 random snapshots (timestamps), starting from the first market creation date.
